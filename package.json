{
  "name": "inplate",
  "version": "0.2.2",
  "description": "A command-line tool processing files with in-place template.",
  "repository": "https://github.com/makeflow/inplate.git",
  "license": "MIT",
  "author": "vilicvane",
  "type": "module",
  "bin": {
    "inplate": "./program/main.js"
  },
  "scripts": {
    "3": "yarn && yarn-deduplicate && yarn",
    "lint": "eslint --no-error-on-unmatched-pattern --report-unused-disable-directives .",
    "lint-prettier": "prettier --check .",
    "test": "npm run lint-prettier && npm run lint && npm run bare-test",
    "bare-test": "NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "dependencies": {
    "chalk": "^5.2.0",
    "commander": "^10.0.1",
    "diff": "^5.1.0",
    "escape-string-regexp": "^5.0.0",
    "glob": "^10.2.4",
    "handlebars": "^4.7.7",
    "html-entities": "^2.3.3",
    "main-function": "^0.1.6"
  },
  "devDependencies": {
    "@mufan/eslint-plugin": "^0.2.19",
    "eslint": "^8.57.1",
    "jest": "^29.5.0",
    "prettier": "^3.5.3",
    "yarn-deduplicate": "^6.0.2"
  }
}
